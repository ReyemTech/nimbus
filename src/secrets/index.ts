/**
 * Secrets module — secret management abstraction.
 *
 * @module secrets
 */

export type { SecretBackend, ISecretRef, ISecretsConfig, ISecrets } from "./interfaces";
export { SECRET_BACKENDS } from "./interfaces";
