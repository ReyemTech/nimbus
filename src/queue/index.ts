/**
 * Queue module — message queue provisioning abstraction.
 *
 * @module queue
 */

export type { QueueEngine, QueueMode, QueueType, IQueueConfig, IQueue } from "./interfaces";
export { QUEUE_ENGINES, QUEUE_MODES, QUEUE_TYPES } from "./interfaces";
