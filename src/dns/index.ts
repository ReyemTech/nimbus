/**
 * DNS module — DNS zone management abstraction.
 *
 * @module dns
 */

export type { DnsRecordType, IDnsRecord, IDnsConfig, IDns } from "./interfaces";
export { DNS_RECORD_TYPES } from "./interfaces";
